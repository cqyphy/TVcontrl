通过adb指令，连接家里电视：
//string connectstr = "adb connect " + TVip, uptimestr = "adb -s " + TVip + " shell uptime", closestr = "adb -s " + TVip + " shell input keyevent 26", getdatestr = "echo %DATE%";
添加了额外txt文件，从而可以得出历史记录，管理每天可看时间
